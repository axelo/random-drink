{
  "name": "random-drink",
  "version": "1.0.0",
  "main": "index.js",
  "author": "axelo",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "serve --single ./public",
    "start:dev": "elm-live --port 3000 --pushstate --dir public src/Main.elm -- --output=public/bundle.js --debug",
    "build": "elm make src/Main.elm --optimize --output=public/bundle.js"
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-live": "^3.0.4"
  },
  "dependencies": {
    "serve": "^10.0.1"
  }
}
